# Notebooks

Colab-Notebooks pro Sitzung. Wird pro Sitzung nach der Veranstaltung ergänzt.

| Notebook | Inhalt |
|---|---|
| `review_radar_demo.ipynb` | Referenz-Implementierung / Hook aus Sitzung 1: die komplette Pipeline (synthetische Reviews → Klassifikation → Themen → Aggregation → Dashboard) plus die Selbstprüfung "stimmt das überhaupt?". Läuft ohne API-Key (Mock), echter Anthropic-Aufruf als Drop-in. |
